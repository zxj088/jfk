# 简单高尔夫记分卡：项目交接说明

更新日期：2026-08-14
当前版本：v6.5.10（PWA 构建 v217）
发布仓库：`https://github.com/zxj088/jfk`

## 产品定位

无需注册登录、通过链接即可使用的移动端高尔夫记分应用，核心只突出三件事：

1. 现场记分。
2. 多人观看正在进行的比赛战况。
3. 查看和分享历史记分卡。

整体体验参考 Golf GameBook 的清晰专业显示，但流程必须更简单，尤其适合老年用户。

## 固定设计原则

- 主色使用深绿、白色和少量暖金色，减少过多浅色背景。
- 卡片圆角适中、少边框，以留白和背景层级区分内容。
- 赛事信息卡显示球场、玩法、日期、洞数进度、领先方和最后更新时间；分享记分卡入口统一放在顶部菜单。
- 领先成绩用醒目的绿色；落后成绩用克制但清晰的红色。
- 记分页面保持大按钮、大数字和固定球员顺序。
- 观看页面以排行榜为第一视觉重点，逐洞记分卡放在下方。
- 历史记分卡采用正式成绩单风格，减少装饰。
- 底部导航最多四项，使用统一线性图标；选中项图标和文字反色显示。
- 中英文切换后保持相同布局，不允许英文挤压按钮。
- 手机屏幕不得横向滚动，必要时增加纵向长度。
- 所有表单仅在用户点击提交、确认或下一步且校验失败后高亮必填或格式错误字段，并自动聚焦首个错误项；用户修正后立即清除提示，初次打开不得提前标红；中英文提示同步且手机端不得产生横向滚动。
- 不增加注册登录、动态、点赞、评论、好友、GPS、击球追踪或复杂赛事管理。

## 业务规则与兼容要求

- 支持拉斯和“斗地主 / Wolf & Pack Scoring”。
- 无编辑权设备进入只读成绩页；编辑权可接管或转交，并使用云端版本号避免覆盖。
- 已完成比赛默认锁定，不应继续显示在“进行中”。
- 本机记住“我是谁”，记分及成绩列表保持稳定顺序；拉斯中同队球员紧随其后。
- 斗地主支持输赢轮换和固定地主；可选择参与比较的农民最佳成绩数量以及平局处理方式；特殊杆倍数以总杆判断，并与手动倍数串接。
- 记分卡前三列统一为“洞、标准杆、难度”，洞信息区域应紧凑且与成绩区有层级区别。
- 第 9 洞后显示小计，第 18 洞后显示加重的总计。
- 低于标准杆使用红色实心圆；高于标准杆 1 杆使用浅蓝实心方块；高于标准杆 2 杆及以上使用深蓝双框方块；标准杆保持普通显示。
- 页面成绩样式和分享 PNG 的成绩样式必须一致。

## 当前发布基线

- 基线标签：`v6.5.10`。
- 线上 PWA 构建：`v217`。
- 正式地址：`https://zxj088.github.io/jfk/?v=217`。
- 原 `golf-score-vegas` v5.1.3 / v180 站点保留不变，不从本项目发布流程覆盖。
- 新比赛球员设置不再显示球员序号标签；拉斯中姓名与差点分两行，斗地主中保持同一行。
- 主页正在进行的多场比赛按比赛 ID 使用固定的克制色标区分，列表顺序变化时颜色不跳变，也不改变比分红绿语义。
- 欢迎页仅在首次使用时显示；返回用户直接进入上次页面。
- 首页头部始终显示“即开即用”，不再因查看过历史比赛而误显示“已锁定”。
- 比赛设置增加总杆、净杆、低于标准杆翻转和编辑密码的直白说明。
- 历史比赛卡片改为更易扫读的多行层级，关键小字、底部导航和语言按钮达到更适合老年用户的字号与触控尺寸。
- 弹窗启用背景隔离、焦点约束和关闭后焦点恢复。
- 成绩页默认使用简洁模式，只突出球员杆数和本洞输赢；用户可切换完整模式查看净杆、组合数字、倍率和地主次数。
- 比赛设置优先显示最近三个球场，并记住上次球场所在国家和地区。
- 历史卡片按球场、日期玩法、最终结果三层显示，只呈现比赛实际采用的计分模式，删除操作收入口菜单。
- 进行中赛事颜色只使用按比赛 ID 固定的左侧色条，卡片主体保持接近白色。
- `/jfk` 的本地状态、历史、球场、语言、设备身份、编辑身份和 PWA 重载标记全部使用 `jfk.` 独立命名空间。
- `/jfk` 与旧站共同使用 Supabase `default` 房间，共享比赛、成绩、历史记录和球场；本地界面状态与设备身份仍完全隔离。
- `/jfk` 使用深蓝球道与金旗的专用 `JFK GOLF` 图标，包括页面品牌标记和 192/512 PWA 安装图标；旧站图标不变。
- 编辑设备经过锁屏、后台暂停或短时弱网后，只要云端 owner 仍为本机，就会保留编辑权并在回到前台后立即续租。
- 编辑权续租采用串行执行，并与待同步成绩写入错开；云端请求超时后可在后续轮询中恢复。
- 分享记分卡入口已从成绩页赛事卡移入顶部菜单；仅完成比赛显示该入口。
- 菜单内规则项去掉问号并移到“关于”上方。
- 斗地主三人或四人球员总分卡固定为同一行显示。
- 拉斯成绩表采用与斗地主一致的中性表格层级；特殊杆实心底色数字在只读手机浏览器中保持白色。
- 拉斯分享 PNG 缩窄洞、标准杆和难度列，增加逐洞行高，扩大球员成绩区域并减小 `EXTRA` 字号。
- 分享弹窗保持单张完整图片、自然高度、仅纵向滚动，不使用裁切或重复图片方案。
- 云端同步启动时只读取最近 7 天比赛摘要和全部进行中比赛；完整比赛仅在打开、继续记分或观看时按需读取。
- 历史时间筛选切换到最近 30 天、全部或自定义范围时按范围补充摘要；旧斗地主摘要缺少逐人积分时显示查看入口和洞数，不伪造零分。
- 编辑锁有效期为 120 秒，编辑设备每 30 秒检查并续租；观看进行中比赛每 30 秒读取当前比赛。完成的历史比赛不持续轮询，页面进入后台时暂停云端轮询，回到前台后立即补同步。
- 斗地主记分页的地主/农民选择按钮固定为单行；倍数说明放在按钮列下方并缩小字号，结果卡以角色图标替代重复的地主/农民文字。
- 经编辑码或通用码 `59` 确认后，比赛和自定义球场直接从 Supabase 物理删除，不再创建或重复上传删除标记。
- 云端已查询范围是本地缓存的权威来源；其他设备同步后会移除数据库中已不存在的比赛和球场，同时保留正在编辑的比赛、待上传比赛和明确排队的球场新增或修改。
- 旧版比赛与球场删除记录会在 v199 启动时从本地存储清除，避免旧 tombstone 再次写回数据库。
- 匿名客户端只保留比赛和球场读取权限，不能直接新增、修改或删除数据库记录。
- 比赛与球场编辑码已迁出公开 JSON；所有云端写入经 `scorecard-write` Edge Function 在服务端验证编辑码或通用码 `59`。
- 接管记分仍只输入一次编辑码，当前设备本地记住凭据；逐洞记分继续本地即时响应并在后台进行版本化上传。
- 连续错误编辑码受到服务端限流；删除仅在云端确认实际删除后才清除本地数据。
- 记分与成绩页面共用系统栏中的单一操作按钮：未取得记分权时显示“接管记分”，取得后显示“结束比赛”；记分页不再重复显示第二个按钮。
- 接管记分成功后自动进入第一处尚未完成的洞，不停留在此前观看的洞位。
- 修复损坏的 192×192 安装图标，并新增使用独立文件名的 180×180 苹果主屏幕图标，避免 iOS 读取损坏资源或继续命中旧图标缓存。
- 斗地主移除单洞封顶设置，改为选择 1 至全部农民中的若干个最佳杆数计算精确平均值，并直接与地主杆数比较；平均值不取整，旧比赛默认继续比较全部农民。
- 斗地主平局可设置为不输不赢、高差点地主获胜、农民获胜或地主获胜；强制胜负继续使用手动倍数与炸弹倍数并保持总积分为零。
- 球员姓名保留浏览器历史候选，并增加明确的“历史”按钮打开历史球员列表；选择后自动带入最近差点，不再使用容易误解的箭头按钮。
- 斗地主分享记分卡顶部完整显示本场球员与差点、球员人数、总杆/净杆、参与比较的农民最佳成绩数量、平局规则、地主方式及固定地主姓名（如适用）。
- 主页有进行中比赛时优先显示进行中列表；没有时隐藏空区域。
- 手机主页“开始记分”占整行，“观看比赛”和“历史记分卡”并排显示。
- 只有一场进行中或历史比赛时入口直接打开；多场时滚动到列表供用户选择，不再误开第一场。
- 比赛卡片明确显示“继续记分 / 观看战况 / 查看记分卡”。
- 历史筛选默认收起，默认显示三场，可展开全部；无匹配结果可一键清除筛选。
- 比赛规则步骤使用直接点选按钮设置玩法、人数、总杆/净杆和地主方式，选中状态即时显示。
- 拉斯固定4人；斗地主支持3人或4人，并以“农民成绩取几人”按钮设置参与比较的最佳农民数量。
- 斗地主比较说明分为地主计算、农民计算和“杆数少的一方获胜”三行小号文字，随人数实时更新。
- 比赛规则页采用紧凑分组和手机端吸底操作栏；366px与390px均无横向滚动。
- 自动测试增加斗地主与拉斯模拟案例，覆盖总杆、净杆、翻转、平局、倍数、累计输赢和零和校验。
- 英文主页三个主入口标题统一放大到 19px，保持 390px 手机宽度无横向滚动。
- 新球局地区筛选兼容 Garmin 的 `Stockholm` 与既有球场数据的 `Stockholm County`，瑞典斯德哥尔摩球场可正常选择。

发布前应通过语法检查、自动测试、生产构建、手机宽度视觉检查和线上 `v217` 资源验证。

## 验证命令

```powershell
node --check app.js
npm.cmd test
npm.cmd run build
```

生产构建会同步 Web 资源到 `www`。移动端重点检查 390–430px 宽度，确认无横向滚动、按钮不挤压、分享预览字号可读。自动化检查直接使用当前工作树的本地 HTTP 预览；不得先尝试 `file://`。复用端口前先核对页面版本，版本不符时改用确认空闲的新端口。

## 发布约定

- 本地实现与验证不等于线上发布。
- 发布前先确认 Git 状态，只提交明确属于本次工作的文件。
- 不提交 `supabase/.temp/` 或重复的本地数据副本。
- 正式 `/jfk` 仓库使用远程名 `origin`；旧站仓库使用只读远程名 `legacy-old`，其推送地址保持禁用。
- 当前 worktree 使用本地发布分支 `codex/jfk-release`，不得在 detached HEAD 状态创建发布提交。
- 推送 `main` 后只查询一次 Pages；若短暂等待后仍未自动创建运行，立即调用 GitHub 官方 Pages Build API，不以重复推送、空提交、文档提交或移动标签触发部署。
- `.codex-remote-attachments/`、`data/shared-courses.seed - 副本.json` 和 `supabase/.temp/` 必须保留且不得暂存；仅在其状态变化或存在误提交风险时对用户重复说明。
- 发布后同时验证线上链接与手机浏览器缓存更新。

## 下一任务建议

新任务开始时先阅读本文件、`v6.5.10` 标签和最新 Git 提交，再围绕单一页面或一组相关功能工作。每组修改完成后依次执行语法检查、自动测试、生产构建和手机宽度视觉检查。
