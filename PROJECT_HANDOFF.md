# 简单高尔夫记分卡：项目交接说明

更新日期：2026-08-05  
当前版本：v6.1.3（PWA 构建 v185）
发布仓库：`https://github.com/zxj088/jfk`

## 产品定位

无需注册登录、通过链接即可使用的移动端高尔夫记分应用，核心只突出三件事：

1. 现场记分。
2. 多人观看正在进行的比赛战况。
3. 查看和分享历史记分卡。

整体体验参考 Golf GameBook 的清晰专业显示，但流程必须更简单，尤其适合老年用户。

## 固定设计原则

- 主色使用深绿、白色和少量暖金色，减少过多浅色背景。
- 卡片圆角适中、少边框，以留白和背景层级区分内容。
- 赛事信息卡显示球场、玩法、日期、洞数进度、领先方和最后更新时间；分享记分卡入口统一放在顶部菜单。
- 领先成绩用醒目的绿色；落后成绩用克制但清晰的红色。
- 记分页面保持大按钮、大数字和固定球员顺序。
- 观看页面以排行榜为第一视觉重点，逐洞记分卡放在下方。
- 历史记分卡采用正式成绩单风格，减少装饰。
- 底部导航最多四项，使用统一线性图标；选中项图标和文字反色显示。
- 中英文切换后保持相同布局，不允许英文挤压按钮。
- 手机屏幕不得横向滚动，必要时增加纵向长度。
- 不增加注册登录、动态、点赞、评论、好友、GPS、击球追踪或复杂赛事管理。

## 业务规则与兼容要求

- 支持拉斯和“斗地主 / Wolf & Pack Scoring”。
- 无编辑权设备进入只读成绩页；编辑权可接管或转交，并使用云端版本号避免覆盖。
- 已完成比赛默认锁定，不应继续显示在“进行中”。
- 本机记住“我是谁”，记分及成绩列表保持稳定顺序；拉斯中同队球员紧随其后。
- 斗地主支持输赢轮换和固定地主；特殊杆倍数以总杆判断，并与手动倍数串接。
- 记分卡前三列统一为“洞、标准杆、难度”，洞信息区域应紧凑且与成绩区有层级区别。
- 第 9 洞后显示小计，第 18 洞后显示加重的总计。
- 低于标准杆使用红色实心圆；高于标准杆 1 杆使用浅蓝实心方块；高于标准杆 2 杆及以上使用深蓝双框方块；标准杆保持普通显示。
- 页面成绩样式和分享 PNG 的成绩样式必须一致。

## 当前发布基线

- 基线标签：`v6.1.3`。
- 线上 PWA 构建：`v185`。
- 正式地址：`https://zxj088.github.io/jfk/?v=185`。
- 原 `golf-score-vegas` v5.1.3 / v180 站点保留不变，不从本项目发布流程覆盖。
- 新比赛球员设置不再显示球员序号标签；拉斯中姓名与差点分两行，斗地主中保持同一行。
- 主页正在进行的多场比赛按比赛 ID 使用固定的克制色标区分，列表顺序变化时颜色不跳变，也不改变比分红绿语义。
- 欢迎页仅在首次使用时显示；返回用户直接进入上次页面。
- 首页头部始终显示“即开即用”，不再因查看过历史比赛而误显示“已锁定”。
- 比赛设置增加总杆、净杆、低于标准杆翻转和编辑密码的直白说明。
- 历史比赛卡片改为更易扫读的多行层级，关键小字、底部导航和语言按钮达到更适合老年用户的字号与触控尺寸。
- 弹窗启用背景隔离、焦点约束和关闭后焦点恢复。
- 成绩页默认使用简洁模式，只突出球员杆数和本洞输赢；用户可切换完整模式查看净杆、组合数字、倍率和地主次数。
- 比赛设置优先显示最近三个球场，并记住上次球场所在国家和地区。
- 历史卡片按球场、日期玩法、最终结果三层显示，只呈现比赛实际采用的计分模式，删除操作收入口菜单。
- 进行中赛事颜色只使用按比赛 ID 固定的左侧色条，卡片主体保持接近白色。
- `/jfk` 的本地状态、历史、球场、语言、设备身份、编辑身份、删除记录和 PWA 重载标记全部使用 `jfk.` 独立命名空间。
- `/jfk` 与旧站共同使用 Supabase `default` 房间，共享比赛、成绩、历史记录和球场；本地界面状态与设备身份仍完全隔离。
- `/jfk` 使用深蓝球道与金旗的专用 `JFK GOLF` 图标，包括页面品牌标记和 192/512 PWA 安装图标；旧站图标不变。
- 编辑设备经过锁屏、后台暂停或短时弱网后，只要云端 owner 仍为本机，就会保留编辑权并在回到前台后立即续租。
- 编辑权续租采用串行执行，并与待同步成绩写入错开；云端请求超时后可在后续轮询中恢复。
- 分享记分卡入口已从成绩页赛事卡移入顶部菜单；仅完成比赛显示该入口。
- 菜单内规则项去掉问号并移到“关于”上方。
- 斗地主三人或四人球员总分卡固定为同一行显示。
- 拉斯成绩表采用与斗地主一致的中性表格层级；特殊杆实心底色数字在只读手机浏览器中保持白色。
- 拉斯分享 PNG 缩窄洞、标准杆和难度列，增加逐洞行高，扩大球员成绩区域并减小 `EXTRA` 字号。
- 分享弹窗保持单张完整图片、自然高度、仅纵向滚动，不使用裁切或重复图片方案。

发布前应通过语法检查、自动测试、生产构建、手机宽度视觉检查和线上 `v185` 资源验证。

## 验证命令

```powershell
node --check app.js
npm.cmd test
npm.cmd run build
```

生产构建会同步 Web 资源到 `www`。移动端重点检查 390–430px 宽度，确认无横向滚动、按钮不挤压、分享预览字号可读。

## 发布约定

- 本地实现与验证不等于线上发布。
- 发布前先确认 Git 状态，只提交明确属于本次工作的文件。
- 不提交 `supabase/.temp/` 或重复的本地数据副本。
- 发布后同时验证线上链接与手机浏览器缓存更新。

## 下一任务建议

新任务开始时先阅读本文件、`v6.1.3` 标签和最新 Git 提交，再围绕单一页面或一组相关功能工作。每组修改完成后依次执行语法检查、自动测试、生产构建和手机宽度视觉检查。
